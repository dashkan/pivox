// Generated by protoc-gen-pivox-swift-facade. DO NOT EDIT.
// source: pivox/ai/v1/ai_chat.proto
// service: pivox.ai.v1.AiChat

import Foundation
import PivoxModels

private final class _StreamCtx_StreamGenerateContent {
    let continuation: AsyncThrowingStream<Pivox_Ai_V1_ServerEvent, Error>.Continuation
    init(continuation: AsyncThrowingStream<Pivox_Ai_V1_ServerEvent, Error>.Continuation) {
        self.continuation = continuation
    }
}

public extension ChatClient {
    // pivox.ai.v1.AiChat.GetConversation
    func getConversation(
        _ request: Pivox_Ai_V1_GetConversationRequest
    ) async throws -> Pivox_Ai_V1_Conversation {
        var req = request
        var resp = Pivox_Ai_V1_Conversation()
        let status = withUnsafePointer(to: &req) { reqPtr in
            withUnsafeMutablePointer(to: &resp) { respPtr in
                cpp.GetConversation(OpaquePointer(reqPtr), OpaquePointer(respPtr))
            }
        }
        if status.code != 0 {
            throw GRPCError(code: status.code, message: String(status.message))
        }
        return resp
    }

    // pivox.ai.v1.AiChat.ListConversations
    func listConversations(
        _ request: Pivox_Ai_V1_ListConversationsRequest
    ) async throws -> Pivox_Ai_V1_ListConversationsResponse {
        var req = request
        var resp = Pivox_Ai_V1_ListConversationsResponse()
        let status = withUnsafePointer(to: &req) { reqPtr in
            withUnsafeMutablePointer(to: &resp) { respPtr in
                cpp.ListConversations(OpaquePointer(reqPtr), OpaquePointer(respPtr))
            }
        }
        if status.code != 0 {
            throw GRPCError(code: status.code, message: String(status.message))
        }
        return resp
    }

    // pivox.ai.v1.AiChat.CreateConversation
    func createConversation(
        _ request: Pivox_Ai_V1_CreateConversationRequest
    ) async throws -> Pivox_Ai_V1_Conversation {
        var req = request
        var resp = Pivox_Ai_V1_Conversation()
        let status = withUnsafePointer(to: &req) { reqPtr in
            withUnsafeMutablePointer(to: &resp) { respPtr in
                cpp.CreateConversation(OpaquePointer(reqPtr), OpaquePointer(respPtr))
            }
        }
        if status.code != 0 {
            throw GRPCError(code: status.code, message: String(status.message))
        }
        return resp
    }

    // pivox.ai.v1.AiChat.UpdateConversation
    func updateConversation(
        _ request: Pivox_Ai_V1_UpdateConversationRequest
    ) async throws -> Pivox_Ai_V1_Conversation {
        var req = request
        var resp = Pivox_Ai_V1_Conversation()
        let status = withUnsafePointer(to: &req) { reqPtr in
            withUnsafeMutablePointer(to: &resp) { respPtr in
                cpp.UpdateConversation(OpaquePointer(reqPtr), OpaquePointer(respPtr))
            }
        }
        if status.code != 0 {
            throw GRPCError(code: status.code, message: String(status.message))
        }
        return resp
    }

    // pivox.ai.v1.AiChat.DeleteConversation — skipped: input/output type lives outside PivoxModels

    // pivox.ai.v1.AiChat.GetMessage
    func getMessage(
        _ request: Pivox_Ai_V1_GetMessageRequest
    ) async throws -> Pivox_Ai_V1_Message {
        var req = request
        var resp = Pivox_Ai_V1_Message()
        let status = withUnsafePointer(to: &req) { reqPtr in
            withUnsafeMutablePointer(to: &resp) { respPtr in
                cpp.GetMessage(OpaquePointer(reqPtr), OpaquePointer(respPtr))
            }
        }
        if status.code != 0 {
            throw GRPCError(code: status.code, message: String(status.message))
        }
        return resp
    }

    // pivox.ai.v1.AiChat.ListMessages
    func listMessages(
        _ request: Pivox_Ai_V1_ListMessagesRequest
    ) async throws -> Pivox_Ai_V1_ListMessagesResponse {
        var req = request
        var resp = Pivox_Ai_V1_ListMessagesResponse()
        let status = withUnsafePointer(to: &req) { reqPtr in
            withUnsafeMutablePointer(to: &resp) { respPtr in
                cpp.ListMessages(OpaquePointer(reqPtr), OpaquePointer(respPtr))
            }
        }
        if status.code != 0 {
            throw GRPCError(code: status.code, message: String(status.message))
        }
        return resp
    }

    // pivox.ai.v1.AiChat.GetArtifact
    func getArtifact(
        _ request: Pivox_Ai_V1_GetArtifactRequest
    ) async throws -> Pivox_Ai_V1_Artifact {
        var req = request
        var resp = Pivox_Ai_V1_Artifact()
        let status = withUnsafePointer(to: &req) { reqPtr in
            withUnsafeMutablePointer(to: &resp) { respPtr in
                cpp.GetArtifact(OpaquePointer(reqPtr), OpaquePointer(respPtr))
            }
        }
        if status.code != 0 {
            throw GRPCError(code: status.code, message: String(status.message))
        }
        return resp
    }

    // pivox.ai.v1.AiChat.ListArtifacts
    func listArtifacts(
        _ request: Pivox_Ai_V1_ListArtifactsRequest
    ) async throws -> Pivox_Ai_V1_ListArtifactsResponse {
        var req = request
        var resp = Pivox_Ai_V1_ListArtifactsResponse()
        let status = withUnsafePointer(to: &req) { reqPtr in
            withUnsafeMutablePointer(to: &resp) { respPtr in
                cpp.ListArtifacts(OpaquePointer(reqPtr), OpaquePointer(respPtr))
            }
        }
        if status.code != 0 {
            throw GRPCError(code: status.code, message: String(status.message))
        }
        return resp
    }

    // pivox.ai.v1.AiChat.DeleteArtifact — skipped: input/output type lives outside PivoxModels

    // pivox.ai.v1.AiChat.GetArtifactVersion
    func getArtifactVersion(
        _ request: Pivox_Ai_V1_GetArtifactVersionRequest
    ) async throws -> Pivox_Ai_V1_ArtifactVersion {
        var req = request
        var resp = Pivox_Ai_V1_ArtifactVersion()
        let status = withUnsafePointer(to: &req) { reqPtr in
            withUnsafeMutablePointer(to: &resp) { respPtr in
                cpp.GetArtifactVersion(OpaquePointer(reqPtr), OpaquePointer(respPtr))
            }
        }
        if status.code != 0 {
            throw GRPCError(code: status.code, message: String(status.message))
        }
        return resp
    }

    // pivox.ai.v1.AiChat.ListArtifactVersions
    func listArtifactVersions(
        _ request: Pivox_Ai_V1_ListArtifactVersionsRequest
    ) async throws -> Pivox_Ai_V1_ListArtifactVersionsResponse {
        var req = request
        var resp = Pivox_Ai_V1_ListArtifactVersionsResponse()
        let status = withUnsafePointer(to: &req) { reqPtr in
            withUnsafeMutablePointer(to: &resp) { respPtr in
                cpp.ListArtifactVersions(OpaquePointer(reqPtr), OpaquePointer(respPtr))
            }
        }
        if status.code != 0 {
            throw GRPCError(code: status.code, message: String(status.message))
        }
        return resp
    }

    // pivox.ai.v1.AiChat.DeleteArtifactVersion — skipped: input/output type lives outside PivoxModels

    // pivox.ai.v1.AiChat.GenerateContent
    func generateContent(
        _ request: Pivox_Ai_V1_GenerateContentRequest
    ) async throws -> Pivox_Ai_V1_GenerateContentResponse {
        var req = request
        var resp = Pivox_Ai_V1_GenerateContentResponse()
        let status = withUnsafePointer(to: &req) { reqPtr in
            withUnsafeMutablePointer(to: &resp) { respPtr in
                cpp.GenerateContent(OpaquePointer(reqPtr), OpaquePointer(respPtr))
            }
        }
        if status.code != 0 {
            throw GRPCError(code: status.code, message: String(status.message))
        }
        return resp
    }

    // pivox.ai.v1.AiChat.StreamGenerateContent
    func streamGenerateContent(
        _ request: Pivox_Ai_V1_GenerateContentRequest
    ) -> AsyncThrowingStream<Pivox_Ai_V1_ServerEvent, Error> {
        return AsyncThrowingStream { continuation in
            let ctx = Unmanaged.passRetained(_StreamCtx_StreamGenerateContent(continuation: continuation))
            var requestCopy = request
            withUnsafePointer(to: &requestCopy) { reqPtr in
                cpp.StreamGenerateContent(
                    OpaquePointer(reqPtr),
                    ctx.toOpaque(),
                    { rawCtx, eventOpaque in
                        guard let rawCtx else { return }
                        let c = Unmanaged<_StreamCtx_StreamGenerateContent>.fromOpaque(rawCtx).takeUnretainedValue()
                        if let op = eventOpaque {
                            let typedPtr = UnsafePointer<Pivox_Ai_V1_ServerEvent>(op)
                            c.continuation.yield(typedPtr.pointee)
                        }
                    },
                    { rawCtx, code, msg in
                        guard let rawCtx else { return }
                        let c = Unmanaged<_StreamCtx_StreamGenerateContent>.fromOpaque(rawCtx).takeRetainedValue()
                        let text = msg.map { String(cString: $0) } ?? "stream error"
                        c.continuation.finish(throwing: GRPCError(code: code, message: text))
                    },
                    { rawCtx in
                        guard let rawCtx else { return }
                        let c = Unmanaged<_StreamCtx_StreamGenerateContent>.fromOpaque(rawCtx).takeRetainedValue()
                        c.continuation.finish()
                    }
                )
            }
            let client = cpp
            continuation.onTermination = { _ in client.Cancel() }
        }
    }

    // pivox.ai.v1.AiChat.SummarizeConversation
    func summarizeConversation(
        _ request: Pivox_Ai_V1_SummarizeConversationRequest
    ) async throws -> Pivox_Ai_V1_Conversation {
        var req = request
        var resp = Pivox_Ai_V1_Conversation()
        let status = withUnsafePointer(to: &req) { reqPtr in
            withUnsafeMutablePointer(to: &resp) { respPtr in
                cpp.SummarizeConversation(OpaquePointer(reqPtr), OpaquePointer(respPtr))
            }
        }
        if status.code != 0 {
            throw GRPCError(code: status.code, message: String(status.message))
        }
        return resp
    }

}
