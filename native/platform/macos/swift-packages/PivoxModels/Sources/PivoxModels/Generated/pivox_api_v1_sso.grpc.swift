// Copyright 2025 Pivox
//
// Licensed under the Apache License, Version 2.0 (the "License");
// you may not use this file except in compliance with the License.
// You may obtain a copy of the License at
//
//     http://www.apache.org/licenses/LICENSE-2.0
//
// Unless required by applicable law or agreed to in writing, software
// distributed under the License is distributed on an "AS IS" BASIS,
// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
// See the License for the specific language governing permissions and
// limitations under the License.

// DO NOT EDIT.
// swift-format-ignore-file
// swiftlint:disable all
//
// Generated by the gRPC Swift generator plugin for the protocol buffer compiler.
// Source: pivox/api/v1/sso.proto
//
// For information on using the generated types, please see the documentation:
//   https://github.com/grpc/grpc-swift

import GRPCCore
import GRPCProtobuf

// MARK: - pivox.api.v1.Sso

/// Namespace containing generated types for the "pivox.api.v1.Sso" service.
@available(macOS 15.0, iOS 18.0, watchOS 11.0, tvOS 18.0, visionOS 2.0, *)
public enum Pivox_Api_V1_Sso: Sendable {
    /// Service descriptor for the "pivox.api.v1.Sso" service.
    public static let descriptor = GRPCCore.ServiceDescriptor(fullyQualifiedService: "pivox.api.v1.Sso")
    /// Namespace for method metadata.
    public enum Method: Sendable {
        /// Namespace for "ResolveProvider" metadata.
        public enum ResolveProvider: Sendable {
            /// Request type for "ResolveProvider".
            public typealias Input = Pivox_Api_V1_ResolveProviderRequest
            /// Response type for "ResolveProvider".
            public typealias Output = Pivox_Api_V1_ResolveProviderResponse
            /// Descriptor for "ResolveProvider".
            public static let descriptor = GRPCCore.MethodDescriptor(
                service: GRPCCore.ServiceDescriptor(fullyQualifiedService: "pivox.api.v1.Sso"),
                method: "ResolveProvider",
                type: .unary
            )
        }
        /// Descriptors for all methods in the "pivox.api.v1.Sso" service.
        public static let descriptors: [GRPCCore.MethodDescriptor] = [
            ResolveProvider.descriptor
        ]
    }
}

@available(macOS 15.0, iOS 18.0, watchOS 11.0, tvOS 18.0, visionOS 2.0, *)
extension GRPCCore.ServiceDescriptor {
    /// Service descriptor for the "pivox.api.v1.Sso" service.
    public static let pivox_api_v1_Sso = GRPCCore.ServiceDescriptor(fullyQualifiedService: "pivox.api.v1.Sso")
}

// MARK: pivox.api.v1.Sso (client)

@available(macOS 15.0, iOS 18.0, watchOS 11.0, tvOS 18.0, visionOS 2.0, *)
extension Pivox_Api_V1_Sso {
    /// Generated client protocol for the "pivox.api.v1.Sso" service.
    ///
    /// You don't need to implement this protocol directly, use the generated
    /// implementation, ``Client``.
    ///
    /// > Source IDL Documentation:
    /// >
    /// > Sso is a small, pre-authentication service that hosts the lookup
    /// > front-channel (`Resolve`) used by the sign-in screen before any
    /// > user identity exists. Authenticated SSO config CRUD lives on the
    /// > Organizations service (Get/UpdateSsoConfig) — that's an AIP-156
    /// > singleton sub-resource of Organization.
    /// > 
    /// > Security profile (uniform across this service):
    /// >   - No Firebase auth required (callers don't have an identity yet).
    /// >   - Per-IP rate-limited at the middleware layer; surfaces as
    /// >     RESOURCE_EXHAUSTED.
    /// >   - Information-disclosure aware: returns NOT_FOUND for unknown
    /// >     domains rather than echoing details back.
    public protocol ClientProtocol: Sendable {
        /// Call the "ResolveProvider" method.
        ///
        /// > Source IDL Documentation:
        /// >
        /// > Maps an email address to its SSO provider id, if any. Used by the
        /// > sign-in screen to decide whether to redirect the user to an
        /// > identity provider before attempting password sign-in.
        /// > 
        /// > Returns NOT_FOUND if no SSO is configured for the email's domain
        /// > (or if the SsoConfig is `enabled=false`). Returns RESOURCE_EXHAUSTED
        /// > if the caller is over the per-IP rate limit.
        /// > 
        /// > (-- api-linter: core::0136::http-uri-suffix=disabled
        /// >     aip.dev/not-precedent: ResolveProvider is a service-level
        /// >     (collection-less) custom method; the URL pattern intentionally
        /// >     has no parent. --)
        ///
        /// - Parameters:
        ///   - request: A request containing a single `Pivox_Api_V1_ResolveProviderRequest` message.
        ///   - serializer: A serializer for `Pivox_Api_V1_ResolveProviderRequest` messages.
        ///   - deserializer: A deserializer for `Pivox_Api_V1_ResolveProviderResponse` messages.
        ///   - options: Options to apply to this RPC.
        ///   - handleResponse: A closure which handles the response, the result of which is
        ///       returned to the caller. Returning from the closure will cancel the RPC if it
        ///       hasn't already finished.
        /// - Returns: The result of `handleResponse`.
        func resolveProvider<Result>(
            request: GRPCCore.ClientRequest<Pivox_Api_V1_ResolveProviderRequest>,
            serializer: some GRPCCore.MessageSerializer<Pivox_Api_V1_ResolveProviderRequest>,
            deserializer: some GRPCCore.MessageDeserializer<Pivox_Api_V1_ResolveProviderResponse>,
            options: GRPCCore.CallOptions,
            onResponse handleResponse: @Sendable @escaping (GRPCCore.ClientResponse<Pivox_Api_V1_ResolveProviderResponse>) async throws -> Result
        ) async throws -> Result where Result: Sendable
    }

    /// Generated client for the "pivox.api.v1.Sso" service.
    ///
    /// The ``Client`` provides an implementation of ``ClientProtocol`` which wraps
    /// a `GRPCCore.GRPCCClient`. The underlying `GRPCClient` provides the long-lived
    /// means of communication with the remote peer.
    ///
    /// > Source IDL Documentation:
    /// >
    /// > Sso is a small, pre-authentication service that hosts the lookup
    /// > front-channel (`Resolve`) used by the sign-in screen before any
    /// > user identity exists. Authenticated SSO config CRUD lives on the
    /// > Organizations service (Get/UpdateSsoConfig) — that's an AIP-156
    /// > singleton sub-resource of Organization.
    /// > 
    /// > Security profile (uniform across this service):
    /// >   - No Firebase auth required (callers don't have an identity yet).
    /// >   - Per-IP rate-limited at the middleware layer; surfaces as
    /// >     RESOURCE_EXHAUSTED.
    /// >   - Information-disclosure aware: returns NOT_FOUND for unknown
    /// >     domains rather than echoing details back.
    public struct Client<Transport>: ClientProtocol where Transport: GRPCCore.ClientTransport {
        private let client: GRPCCore.GRPCClient<Transport>

        /// Creates a new client wrapping the provided `GRPCCore.GRPCClient`.
        ///
        /// - Parameters:
        ///   - client: A `GRPCCore.GRPCClient` providing a communication channel to the service.
        public init(wrapping client: GRPCCore.GRPCClient<Transport>) {
            self.client = client
        }

        /// Call the "ResolveProvider" method.
        ///
        /// > Source IDL Documentation:
        /// >
        /// > Maps an email address to its SSO provider id, if any. Used by the
        /// > sign-in screen to decide whether to redirect the user to an
        /// > identity provider before attempting password sign-in.
        /// > 
        /// > Returns NOT_FOUND if no SSO is configured for the email's domain
        /// > (or if the SsoConfig is `enabled=false`). Returns RESOURCE_EXHAUSTED
        /// > if the caller is over the per-IP rate limit.
        /// > 
        /// > (-- api-linter: core::0136::http-uri-suffix=disabled
        /// >     aip.dev/not-precedent: ResolveProvider is a service-level
        /// >     (collection-less) custom method; the URL pattern intentionally
        /// >     has no parent. --)
        ///
        /// - Parameters:
        ///   - request: A request containing a single `Pivox_Api_V1_ResolveProviderRequest` message.
        ///   - serializer: A serializer for `Pivox_Api_V1_ResolveProviderRequest` messages.
        ///   - deserializer: A deserializer for `Pivox_Api_V1_ResolveProviderResponse` messages.
        ///   - options: Options to apply to this RPC.
        ///   - handleResponse: A closure which handles the response, the result of which is
        ///       returned to the caller. Returning from the closure will cancel the RPC if it
        ///       hasn't already finished.
        /// - Returns: The result of `handleResponse`.
        public func resolveProvider<Result>(
            request: GRPCCore.ClientRequest<Pivox_Api_V1_ResolveProviderRequest>,
            serializer: some GRPCCore.MessageSerializer<Pivox_Api_V1_ResolveProviderRequest>,
            deserializer: some GRPCCore.MessageDeserializer<Pivox_Api_V1_ResolveProviderResponse>,
            options: GRPCCore.CallOptions = .defaults,
            onResponse handleResponse: @Sendable @escaping (GRPCCore.ClientResponse<Pivox_Api_V1_ResolveProviderResponse>) async throws -> Result = { response in
                try response.message
            }
        ) async throws -> Result where Result: Sendable {
            try await self.client.unary(
                request: request,
                descriptor: Pivox_Api_V1_Sso.Method.ResolveProvider.descriptor,
                serializer: serializer,
                deserializer: deserializer,
                options: options,
                onResponse: handleResponse
            )
        }
    }
}

// Helpers providing default arguments to 'ClientProtocol' methods.
@available(macOS 15.0, iOS 18.0, watchOS 11.0, tvOS 18.0, visionOS 2.0, *)
extension Pivox_Api_V1_Sso.ClientProtocol {
    /// Call the "ResolveProvider" method.
    ///
    /// > Source IDL Documentation:
    /// >
    /// > Maps an email address to its SSO provider id, if any. Used by the
    /// > sign-in screen to decide whether to redirect the user to an
    /// > identity provider before attempting password sign-in.
    /// > 
    /// > Returns NOT_FOUND if no SSO is configured for the email's domain
    /// > (or if the SsoConfig is `enabled=false`). Returns RESOURCE_EXHAUSTED
    /// > if the caller is over the per-IP rate limit.
    /// > 
    /// > (-- api-linter: core::0136::http-uri-suffix=disabled
    /// >     aip.dev/not-precedent: ResolveProvider is a service-level
    /// >     (collection-less) custom method; the URL pattern intentionally
    /// >     has no parent. --)
    ///
    /// - Parameters:
    ///   - request: A request containing a single `Pivox_Api_V1_ResolveProviderRequest` message.
    ///   - options: Options to apply to this RPC.
    ///   - handleResponse: A closure which handles the response, the result of which is
    ///       returned to the caller. Returning from the closure will cancel the RPC if it
    ///       hasn't already finished.
    /// - Returns: The result of `handleResponse`.
    public func resolveProvider<Result>(
        request: GRPCCore.ClientRequest<Pivox_Api_V1_ResolveProviderRequest>,
        options: GRPCCore.CallOptions = .defaults,
        onResponse handleResponse: @Sendable @escaping (GRPCCore.ClientResponse<Pivox_Api_V1_ResolveProviderResponse>) async throws -> Result = { response in
            try response.message
        }
    ) async throws -> Result where Result: Sendable {
        try await self.resolveProvider(
            request: request,
            serializer: GRPCProtobuf.ProtobufSerializer<Pivox_Api_V1_ResolveProviderRequest>(),
            deserializer: GRPCProtobuf.ProtobufDeserializer<Pivox_Api_V1_ResolveProviderResponse>(),
            options: options,
            onResponse: handleResponse
        )
    }
}

// Helpers providing sugared APIs for 'ClientProtocol' methods.
@available(macOS 15.0, iOS 18.0, watchOS 11.0, tvOS 18.0, visionOS 2.0, *)
extension Pivox_Api_V1_Sso.ClientProtocol {
    /// Call the "ResolveProvider" method.
    ///
    /// > Source IDL Documentation:
    /// >
    /// > Maps an email address to its SSO provider id, if any. Used by the
    /// > sign-in screen to decide whether to redirect the user to an
    /// > identity provider before attempting password sign-in.
    /// > 
    /// > Returns NOT_FOUND if no SSO is configured for the email's domain
    /// > (or if the SsoConfig is `enabled=false`). Returns RESOURCE_EXHAUSTED
    /// > if the caller is over the per-IP rate limit.
    /// > 
    /// > (-- api-linter: core::0136::http-uri-suffix=disabled
    /// >     aip.dev/not-precedent: ResolveProvider is a service-level
    /// >     (collection-less) custom method; the URL pattern intentionally
    /// >     has no parent. --)
    ///
    /// - Parameters:
    ///   - message: request message to send.
    ///   - metadata: Additional metadata to send, defaults to empty.
    ///   - options: Options to apply to this RPC, defaults to `.defaults`.
    ///   - handleResponse: A closure which handles the response, the result of which is
    ///       returned to the caller. Returning from the closure will cancel the RPC if it
    ///       hasn't already finished.
    /// - Returns: The result of `handleResponse`.
    public func resolveProvider<Result>(
        _ message: Pivox_Api_V1_ResolveProviderRequest,
        metadata: GRPCCore.Metadata = [:],
        options: GRPCCore.CallOptions = .defaults,
        onResponse handleResponse: @Sendable @escaping (GRPCCore.ClientResponse<Pivox_Api_V1_ResolveProviderResponse>) async throws -> Result = { response in
            try response.message
        }
    ) async throws -> Result where Result: Sendable {
        let request = GRPCCore.ClientRequest<Pivox_Api_V1_ResolveProviderRequest>(
            message: message,
            metadata: metadata
        )
        return try await self.resolveProvider(
            request: request,
            options: options,
            onResponse: handleResponse
        )
    }
}