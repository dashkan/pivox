// Generated by protoc-gen-pivox-cpp-bridge. DO NOT EDIT.
// service: pivox.ai.v1.AiChat
//
// Method implementations. Include in the translation unit that
// defines ChatClient, guarded by #if defined(__APPLE__).
// Assumes the surrounding TU provides:
//   - `SwiftArrayToVector(swift::Array<uint8_t>) -> std::vector<uint8_t>`
//   - `StartStreamBytes(...)` and `DoUnaryCall(...)` transport helpers
//     on ChatClient (or a shared base).

// pivox.ai.v1.AiChat.GetConversation
UnaryStatus ChatClient::GetConversation(
    const PivoxModels::Pivox_Ai_V1_GetConversationRequest* request,
    PivoxModels::Pivox_Ai_V1_Conversation* response_out) {
  if (!request || !response_out) {
    return UnaryStatus{3 /* INVALID_ARGUMENT */, "null request or response_out"};
  }

  auto req_bytes = SwiftArrayToVector(request->serializedBytesArray());
  std::vector<uint8_t> resp_bytes;
  auto status = DoUnaryCall("/pivox.ai.v1.AiChat/GetConversation",
      req_bytes.data(), req_bytes.size(), &resp_bytes);

  if (!status.ok()) {
    return UnaryStatus{static_cast<int32_t>(status.error_code()), status.error_message()};
  }

  auto arr = swift::Array<uint8_t>::init();
  for (auto b : resp_bytes) arr.append(b);
  *response_out = PivoxModels::Pivox_Ai_V1_Conversation::parse(arr);
  return UnaryStatus{0, std::string{}};
}

// pivox.ai.v1.AiChat.ListConversations
UnaryStatus ChatClient::ListConversations(
    const PivoxModels::Pivox_Ai_V1_ListConversationsRequest* request,
    PivoxModels::Pivox_Ai_V1_ListConversationsResponse* response_out) {
  if (!request || !response_out) {
    return UnaryStatus{3 /* INVALID_ARGUMENT */, "null request or response_out"};
  }

  auto req_bytes = SwiftArrayToVector(request->serializedBytesArray());
  std::vector<uint8_t> resp_bytes;
  auto status = DoUnaryCall("/pivox.ai.v1.AiChat/ListConversations",
      req_bytes.data(), req_bytes.size(), &resp_bytes);

  if (!status.ok()) {
    return UnaryStatus{static_cast<int32_t>(status.error_code()), status.error_message()};
  }

  auto arr = swift::Array<uint8_t>::init();
  for (auto b : resp_bytes) arr.append(b);
  *response_out = PivoxModels::Pivox_Ai_V1_ListConversationsResponse::parse(arr);
  return UnaryStatus{0, std::string{}};
}

// pivox.ai.v1.AiChat.CreateConversation
UnaryStatus ChatClient::CreateConversation(
    const PivoxModels::Pivox_Ai_V1_CreateConversationRequest* request,
    PivoxModels::Pivox_Ai_V1_Conversation* response_out) {
  if (!request || !response_out) {
    return UnaryStatus{3 /* INVALID_ARGUMENT */, "null request or response_out"};
  }

  auto req_bytes = SwiftArrayToVector(request->serializedBytesArray());
  std::vector<uint8_t> resp_bytes;
  auto status = DoUnaryCall("/pivox.ai.v1.AiChat/CreateConversation",
      req_bytes.data(), req_bytes.size(), &resp_bytes);

  if (!status.ok()) {
    return UnaryStatus{static_cast<int32_t>(status.error_code()), status.error_message()};
  }

  auto arr = swift::Array<uint8_t>::init();
  for (auto b : resp_bytes) arr.append(b);
  *response_out = PivoxModels::Pivox_Ai_V1_Conversation::parse(arr);
  return UnaryStatus{0, std::string{}};
}

// pivox.ai.v1.AiChat.UpdateConversation
UnaryStatus ChatClient::UpdateConversation(
    const PivoxModels::Pivox_Ai_V1_UpdateConversationRequest* request,
    PivoxModels::Pivox_Ai_V1_Conversation* response_out) {
  if (!request || !response_out) {
    return UnaryStatus{3 /* INVALID_ARGUMENT */, "null request or response_out"};
  }

  auto req_bytes = SwiftArrayToVector(request->serializedBytesArray());
  std::vector<uint8_t> resp_bytes;
  auto status = DoUnaryCall("/pivox.ai.v1.AiChat/UpdateConversation",
      req_bytes.data(), req_bytes.size(), &resp_bytes);

  if (!status.ok()) {
    return UnaryStatus{static_cast<int32_t>(status.error_code()), status.error_message()};
  }

  auto arr = swift::Array<uint8_t>::init();
  for (auto b : resp_bytes) arr.append(b);
  *response_out = PivoxModels::Pivox_Ai_V1_Conversation::parse(arr);
  return UnaryStatus{0, std::string{}};
}

// pivox.ai.v1.AiChat.GetMessage
UnaryStatus ChatClient::GetMessage(
    const PivoxModels::Pivox_Ai_V1_GetMessageRequest* request,
    PivoxModels::Pivox_Ai_V1_Message* response_out) {
  if (!request || !response_out) {
    return UnaryStatus{3 /* INVALID_ARGUMENT */, "null request or response_out"};
  }

  auto req_bytes = SwiftArrayToVector(request->serializedBytesArray());
  std::vector<uint8_t> resp_bytes;
  auto status = DoUnaryCall("/pivox.ai.v1.AiChat/GetMessage",
      req_bytes.data(), req_bytes.size(), &resp_bytes);

  if (!status.ok()) {
    return UnaryStatus{static_cast<int32_t>(status.error_code()), status.error_message()};
  }

  auto arr = swift::Array<uint8_t>::init();
  for (auto b : resp_bytes) arr.append(b);
  *response_out = PivoxModels::Pivox_Ai_V1_Message::parse(arr);
  return UnaryStatus{0, std::string{}};
}

// pivox.ai.v1.AiChat.ListMessages
UnaryStatus ChatClient::ListMessages(
    const PivoxModels::Pivox_Ai_V1_ListMessagesRequest* request,
    PivoxModels::Pivox_Ai_V1_ListMessagesResponse* response_out) {
  if (!request || !response_out) {
    return UnaryStatus{3 /* INVALID_ARGUMENT */, "null request or response_out"};
  }

  auto req_bytes = SwiftArrayToVector(request->serializedBytesArray());
  std::vector<uint8_t> resp_bytes;
  auto status = DoUnaryCall("/pivox.ai.v1.AiChat/ListMessages",
      req_bytes.data(), req_bytes.size(), &resp_bytes);

  if (!status.ok()) {
    return UnaryStatus{static_cast<int32_t>(status.error_code()), status.error_message()};
  }

  auto arr = swift::Array<uint8_t>::init();
  for (auto b : resp_bytes) arr.append(b);
  *response_out = PivoxModels::Pivox_Ai_V1_ListMessagesResponse::parse(arr);
  return UnaryStatus{0, std::string{}};
}

// pivox.ai.v1.AiChat.GetArtifact
UnaryStatus ChatClient::GetArtifact(
    const PivoxModels::Pivox_Ai_V1_GetArtifactRequest* request,
    PivoxModels::Pivox_Ai_V1_Artifact* response_out) {
  if (!request || !response_out) {
    return UnaryStatus{3 /* INVALID_ARGUMENT */, "null request or response_out"};
  }

  auto req_bytes = SwiftArrayToVector(request->serializedBytesArray());
  std::vector<uint8_t> resp_bytes;
  auto status = DoUnaryCall("/pivox.ai.v1.AiChat/GetArtifact",
      req_bytes.data(), req_bytes.size(), &resp_bytes);

  if (!status.ok()) {
    return UnaryStatus{static_cast<int32_t>(status.error_code()), status.error_message()};
  }

  auto arr = swift::Array<uint8_t>::init();
  for (auto b : resp_bytes) arr.append(b);
  *response_out = PivoxModels::Pivox_Ai_V1_Artifact::parse(arr);
  return UnaryStatus{0, std::string{}};
}

// pivox.ai.v1.AiChat.ListArtifacts
UnaryStatus ChatClient::ListArtifacts(
    const PivoxModels::Pivox_Ai_V1_ListArtifactsRequest* request,
    PivoxModels::Pivox_Ai_V1_ListArtifactsResponse* response_out) {
  if (!request || !response_out) {
    return UnaryStatus{3 /* INVALID_ARGUMENT */, "null request or response_out"};
  }

  auto req_bytes = SwiftArrayToVector(request->serializedBytesArray());
  std::vector<uint8_t> resp_bytes;
  auto status = DoUnaryCall("/pivox.ai.v1.AiChat/ListArtifacts",
      req_bytes.data(), req_bytes.size(), &resp_bytes);

  if (!status.ok()) {
    return UnaryStatus{static_cast<int32_t>(status.error_code()), status.error_message()};
  }

  auto arr = swift::Array<uint8_t>::init();
  for (auto b : resp_bytes) arr.append(b);
  *response_out = PivoxModels::Pivox_Ai_V1_ListArtifactsResponse::parse(arr);
  return UnaryStatus{0, std::string{}};
}

// pivox.ai.v1.AiChat.GetArtifactVersion
UnaryStatus ChatClient::GetArtifactVersion(
    const PivoxModels::Pivox_Ai_V1_GetArtifactVersionRequest* request,
    PivoxModels::Pivox_Ai_V1_ArtifactVersion* response_out) {
  if (!request || !response_out) {
    return UnaryStatus{3 /* INVALID_ARGUMENT */, "null request or response_out"};
  }

  auto req_bytes = SwiftArrayToVector(request->serializedBytesArray());
  std::vector<uint8_t> resp_bytes;
  auto status = DoUnaryCall("/pivox.ai.v1.AiChat/GetArtifactVersion",
      req_bytes.data(), req_bytes.size(), &resp_bytes);

  if (!status.ok()) {
    return UnaryStatus{static_cast<int32_t>(status.error_code()), status.error_message()};
  }

  auto arr = swift::Array<uint8_t>::init();
  for (auto b : resp_bytes) arr.append(b);
  *response_out = PivoxModels::Pivox_Ai_V1_ArtifactVersion::parse(arr);
  return UnaryStatus{0, std::string{}};
}

// pivox.ai.v1.AiChat.ListArtifactVersions
UnaryStatus ChatClient::ListArtifactVersions(
    const PivoxModels::Pivox_Ai_V1_ListArtifactVersionsRequest* request,
    PivoxModels::Pivox_Ai_V1_ListArtifactVersionsResponse* response_out) {
  if (!request || !response_out) {
    return UnaryStatus{3 /* INVALID_ARGUMENT */, "null request or response_out"};
  }

  auto req_bytes = SwiftArrayToVector(request->serializedBytesArray());
  std::vector<uint8_t> resp_bytes;
  auto status = DoUnaryCall("/pivox.ai.v1.AiChat/ListArtifactVersions",
      req_bytes.data(), req_bytes.size(), &resp_bytes);

  if (!status.ok()) {
    return UnaryStatus{static_cast<int32_t>(status.error_code()), status.error_message()};
  }

  auto arr = swift::Array<uint8_t>::init();
  for (auto b : resp_bytes) arr.append(b);
  *response_out = PivoxModels::Pivox_Ai_V1_ListArtifactVersionsResponse::parse(arr);
  return UnaryStatus{0, std::string{}};
}

// pivox.ai.v1.AiChat.GenerateContent
UnaryStatus ChatClient::GenerateContent(
    const PivoxModels::Pivox_Ai_V1_GenerateContentRequest* request,
    PivoxModels::Pivox_Ai_V1_GenerateContentResponse* response_out) {
  if (!request || !response_out) {
    return UnaryStatus{3 /* INVALID_ARGUMENT */, "null request or response_out"};
  }

  auto req_bytes = SwiftArrayToVector(request->serializedBytesArray());
  std::vector<uint8_t> resp_bytes;
  auto status = DoUnaryCall("/pivox.ai.v1.AiChat/GenerateContent",
      req_bytes.data(), req_bytes.size(), &resp_bytes);

  if (!status.ok()) {
    return UnaryStatus{static_cast<int32_t>(status.error_code()), status.error_message()};
  }

  auto arr = swift::Array<uint8_t>::init();
  for (auto b : resp_bytes) arr.append(b);
  *response_out = PivoxModels::Pivox_Ai_V1_GenerateContentResponse::parse(arr);
  return UnaryStatus{0, std::string{}};
}

// pivox.ai.v1.AiChat.StreamGenerateContent
void ChatClient::StreamGenerateContent(
    const PivoxModels::Pivox_Ai_V1_GenerateContentRequest* request,
    void* ctx,
    void (*on_event)(void* ctx, const PivoxModels::Pivox_Ai_V1_ServerEvent* event),
    void (*on_error)(void* ctx, int32_t code, const char* message),
    void (*on_complete)(void* ctx)) {
  if (!request) {
    if (on_error) on_error(ctx, 3 /* INVALID_ARGUMENT */, "null request");
    return;
  }
  auto bytes = SwiftArrayToVector(request->serializedBytesArray());
  StartStreamBytes(
      bytes.data(), bytes.size(),
      [ctx, on_event](const uint8_t* bs, size_t n) {
        if (!on_event) return;
        auto arr = swift::Array<uint8_t>::init();
        for (size_t i = 0; i < n; ++i) arr.append(bs[i]);
        auto ev = PivoxModels::Pivox_Ai_V1_ServerEvent::parse(arr);
        on_event(ctx, &ev);
      },
      [ctx, on_error](const std::string& msg) {
        if (on_error) on_error(ctx, 2 /* UNKNOWN */, msg.c_str());
      },
      [ctx, on_complete]() {
        if (on_complete) on_complete(ctx);
      });
}

// pivox.ai.v1.AiChat.SummarizeConversation
UnaryStatus ChatClient::SummarizeConversation(
    const PivoxModels::Pivox_Ai_V1_SummarizeConversationRequest* request,
    PivoxModels::Pivox_Ai_V1_Conversation* response_out) {
  if (!request || !response_out) {
    return UnaryStatus{3 /* INVALID_ARGUMENT */, "null request or response_out"};
  }

  auto req_bytes = SwiftArrayToVector(request->serializedBytesArray());
  std::vector<uint8_t> resp_bytes;
  auto status = DoUnaryCall("/pivox.ai.v1.AiChat/SummarizeConversation",
      req_bytes.data(), req_bytes.size(), &resp_bytes);

  if (!status.ok()) {
    return UnaryStatus{static_cast<int32_t>(status.error_code()), status.error_message()};
  }

  auto arr = swift::Array<uint8_t>::init();
  for (auto b : resp_bytes) arr.append(b);
  *response_out = PivoxModels::Pivox_Ai_V1_Conversation::parse(arr);
  return UnaryStatus{0, std::string{}};
}

