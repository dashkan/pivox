<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<key>CFBundleName</key>
	<string>Pivox</string>
	<!-- Matches the Swift app's BUNDLE_ID in GoogleService-Info.plist.
	     Firebase API key is restricted to this bundle ID at the
	     Google Cloud Console level — requests from other bundle IDs
	     get rejected. -->
	<key>CFBundleIdentifier</key>
	<string>app.pivox.native</string>
	<key>CFBundleShortVersionString</key>
	<string>1.0</string>
	<key>CFBundleVersion</key>
	<string>1</string>
	<key>CFBundleDevelopmentRegion</key>
	<string>en</string>
	<key>CFBundleInfoDictionaryVersion</key>
	<string>6.0</string>
	<key>CFBundlePackageType</key>
	<string>APPL</string>
	<key>CFBundleSignature</key>
	<string>????</string>
	<key>NSHumanReadableCopyright</key>
	<string>${AuthorCopyright:HtmlEncode}</string>
	<key>NSPrincipalClass</key>
	<string>NSApplication</string>
	<key>XSAppIconAssets</key>
	<string>Assets.xcassets/AppIcon.appiconset</string>
	<!-- Tells macOS to use Assets.xcassets/AccentColor.colorset as the
	     app's accent color (resolves to its violet under the
	     "Multicolor" system accent pref; system accent takes
	     precedence otherwise).
	     Required on .NET-for-macOS: unlike Xcode-built apps, the
	     build doesn't auto-inject this key from the asset name, so
	     macOS doesn't auto-discover the AccentColor.colorset without
	     this declaration. Verified by removing it — accent falls back
	     to system blue. -->
	<key>NSAccentColorName</key>
	<string>AccentColor</string>
</dict>
</plist>
