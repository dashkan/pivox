// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: users.sql

package db

import (
	"context"

	"github.com/google/uuid"
	"github.com/jackc/pgx/v5/pgtype"
)

const countOrgOwnersExcludingUser = `-- name: CountOrgOwnersExcludingUser :one
SELECT COUNT(*)
  FROM org_members om
  JOIN roles r ON r.id = om.role_id
 WHERE om.org_id = $1
   AND r.is_system = true
   AND r.name = 'owner'
   -- IS DISTINCT FROM treats NULL as a value, so group-owner rows
   -- (where om.user_id IS NULL) survive — ` + "`" + `NULL = $2` + "`" + ` would yield
   -- NULL and silently drop them, breaking the sole-owner guard.
   AND (om.user_id IS DISTINCT FROM $2)
   AND (
     om.user_id IS NOT NULL
     OR
     (om.group_id IS NOT NULL
      AND EXISTS (
        SELECT 1 FROM groups g
         WHERE g.id = om.group_id
           AND g.state = 'ACTIVE'))
   )
`

type CountOrgOwnersExcludingUserParams struct {
	OrgID  uuid.UUID   `json:"org_id"`
	UserID pgtype.UUID `json:"user_id"`
}

// CountOrgOwnersExcludingUser is the org-local sole-owner guard for
// Iam.DeleteUser: it counts owner bindings in this org EXCLUDING the
// user being removed, so the handler can refuse if removing them
// would leave the org with zero owners. Counts both user and
// ACTIVE-group principals (matches CountOwnersByOrg).
// $2 is `identities.id`.
func (q *Queries) CountOrgOwnersExcludingUser(ctx context.Context, arg CountOrgOwnersExcludingUserParams) (int64, error) {
	row := q.db.QueryRow(ctx, countOrgOwnersExcludingUser, arg.OrgID, arg.UserID)
	var count int64
	err := row.Scan(&count)
	return count, err
}

const countOwnersByOrg = `-- name: CountOwnersByOrg :one
SELECT COUNT(*)
  FROM org_members om
  JOIN roles r ON r.id = om.role_id
 WHERE om.org_id = $1
   AND r.is_system = true
   AND r.name = 'owner'
   AND (
     om.user_id IS NOT NULL
     OR
     (om.group_id IS NOT NULL
      AND EXISTS (
        SELECT 1 FROM groups g
         WHERE g.id = om.group_id
           AND g.state = 'ACTIVE'))
   )
`

// Counts org_members rows whose role is the system 'owner' role for
// this org, regardless of principal kind. Used by membership-mutation
// handlers to enforce "≥1 owner". Group principals are counted only
// when the group itself is ACTIVE (a soft-deleted group's binding
// doesn't keep the org owner-ful). User principals are counted
// unconditionally — an identity row is preserved across soft-delete
// but the membership cascade in DeleteAccount removes their
// org_members row before tombstoning, so a counted user binding
// always points at a live identity.
func (q *Queries) CountOwnersByOrg(ctx context.Context, orgID uuid.UUID) (int64, error) {
	row := q.db.QueryRow(ctx, countOwnersByOrg, orgID)
	var count int64
	err := row.Scan(&count)
	return count, err
}

const deleteGroupMembersForIdentity = `-- name: DeleteGroupMembersForIdentity :exec
DELETE FROM group_members WHERE user_id = $1
`

// DeleteGroupMembersForIdentity removes the identity from every
// group it belongs to, across all orgs. Cross-org variant for
// DeleteAccount. group_members.user_id is `identities.id` directly,
// so this is a single straight DELETE.
func (q *Queries) DeleteGroupMembersForIdentity(ctx context.Context, userID uuid.UUID) error {
	_, err := q.db.Exec(ctx, deleteGroupMembersForIdentity, userID)
	return err
}

const deleteGroupMembersForUserInOrg = `-- name: DeleteGroupMembersForUserInOrg :exec
DELETE FROM group_members
 WHERE user_id = $2
   AND group_id IN (SELECT id FROM groups WHERE org_id = $1)
`

type DeleteGroupMembersForUserInOrgParams struct {
	OrgID  uuid.UUID `json:"org_id"`
	UserID uuid.UUID `json:"user_id"`
}

// DeleteGroupMembersForUserInOrg removes the user's membership in
// groups belonging to this org. group_members.user_id IS
// identities.id (post-Phase-7).
// $2 is `identities.id`.
func (q *Queries) DeleteGroupMembersForUserInOrg(ctx context.Context, arg DeleteGroupMembersForUserInOrgParams) error {
	_, err := q.db.Exec(ctx, deleteGroupMembersForUserInOrg, arg.OrgID, arg.UserID)
	return err
}

const deleteOrgMembersForIdentity = `-- name: DeleteOrgMembersForIdentity :exec
DELETE FROM org_members WHERE user_id = $1
`

// DeleteOrgMembersForIdentity removes ALL org-scope role bindings
// across every org for this identity (direct user bindings only —
// group-derived bindings stay because the group itself isn't being
// deleted; the user just gets removed from the group via
// DeleteGroupMembersForIdentity below). Used by DeleteAccount's
// cross-org cascade.
func (q *Queries) DeleteOrgMembersForIdentity(ctx context.Context, userID pgtype.UUID) error {
	_, err := q.db.Exec(ctx, deleteOrgMembersForIdentity, userID)
	return err
}

const deleteOrgMembersForUserInOrg = `-- name: DeleteOrgMembersForUserInOrg :exec

DELETE FROM org_members
 WHERE org_id = $1
   AND user_id = $2
`

type DeleteOrgMembersForUserInOrgParams struct {
	OrgID  uuid.UUID   `json:"org_id"`
	UserID pgtype.UUID `json:"user_id"`
}

// ===========================================================================
// Org-scoped cascade queries used by Iam.DeleteUser (org-scoped, sync
// post-Phase-7 — no LRO, no grace). They remove a single user's
// bindings within ONE org, leaving every other org untouched.
// ===========================================================================
// DeleteOrgMembersForUserInOrg removes the user's org-scope role
// bindings in a single org. Bounded by (org_id, user_id).
// $2 is `identities.id`.
func (q *Queries) DeleteOrgMembersForUserInOrg(ctx context.Context, arg DeleteOrgMembersForUserInOrgParams) error {
	_, err := q.db.Exec(ctx, deleteOrgMembersForUserInOrg, arg.OrgID, arg.UserID)
	return err
}

const deleteSpaceMembersForIdentity = `-- name: DeleteSpaceMembersForIdentity :exec
DELETE FROM space_members WHERE user_id = $1
`

// DeleteSpaceMembersForIdentity is the cross-org space-scope
// analogue used by DeleteAccount.
func (q *Queries) DeleteSpaceMembersForIdentity(ctx context.Context, userID pgtype.UUID) error {
	_, err := q.db.Exec(ctx, deleteSpaceMembersForIdentity, userID)
	return err
}

const deleteSpaceMembersForUserInOrg = `-- name: DeleteSpaceMembersForUserInOrg :exec
DELETE FROM space_members
 WHERE user_id = $2
   AND space_id IN (SELECT id FROM spaces WHERE org_id = $1)
`

type DeleteSpaceMembersForUserInOrgParams struct {
	OrgID  uuid.UUID   `json:"org_id"`
	UserID pgtype.UUID `json:"user_id"`
}

// DeleteSpaceMembersForUserInOrg removes the user's space-scope
// bindings for spaces in this org. Joins to spaces to bound by
// org_id, since space_members rows themselves only carry space_id.
// $2 is `identities.id`.
func (q *Queries) DeleteSpaceMembersForUserInOrg(ctx context.Context, arg DeleteSpaceMembersForUserInOrgParams) error {
	_, err := q.db.Exec(ctx, deleteSpaceMembersForUserInOrg, arg.OrgID, arg.UserID)
	return err
}

const hardDeleteIdentity = `-- name: HardDeleteIdentity :exec
DELETE FROM identities WHERE id = $1
`

// HardDeleteIdentity removes the identity row entirely. Operator-only
// — the public DeleteAccount LRO uses SoftDeleteIdentity (which
// preserves the row so historical *_by audit references continue to
// resolve as is_deleted=true). HardDelete remains for terminal
// purges only.
func (q *Queries) HardDeleteIdentity(ctx context.Context, id uuid.UUID) error {
	_, err := q.db.Exec(ctx, hardDeleteIdentity, id)
	return err
}

const listOrganizationsForIdentity = `-- name: ListOrganizationsForIdentity :many

SELECT DISTINCT o.id, o.name, o.display_name, o.annotations, o.state, o.etag, o.revision, o.created_by, o.updated_by, o.deleted_by, o.create_time, o.update_time, o.delete_time, o.purge_time
  FROM organizations o
  JOIN org_members om ON om.org_id = o.id
 WHERE (
   om.user_id = $1
   OR
   om.group_id IN (
     SELECT gm.group_id FROM group_members gm WHERE gm.user_id = $1
   )
 )
 ORDER BY o.id ASC
 LIMIT 1000
`

// Phase 7 unified per-org identity with `identities`. The per-org
// `users` join table was dropped; queries that used to resolve
// "users.id from (org, identity)" now reference `identities.id`
// directly. The handler reads it from the `pivox_user_id` ID-token
// claim — no DB lookup required.
//
// Post-principal-split (Phase 3 of the identities rework): the
// polymorphic `principal_kind/principal_id` pair on org_members /
// space_members was replaced by typed `user_id`/`group_id`
// columns with an XOR check. Direct user binding queries below
// read `user_id` directly; group-mediated lookups read `group_id`.
// Lists all orgs the given identity has membership in, counting
// both direct user bindings AND group-mediated bindings (the user
// is in a group that has an org_members row).
// Caller-scoped for `ListOrganizations` and the
// membership-required interceptor — every authenticated user is
// only ever shown orgs they actually belong to. Includes
// soft-deleted orgs so an owner can reach UndeleteOrganization
// during the 30-day grace window. Excludes purged orgs (the JOIN
// naturally drops those once their org row is gone).
func (q *Queries) ListOrganizationsForIdentity(ctx context.Context, userID pgtype.UUID) ([]Organization, error) {
	rows, err := q.db.Query(ctx, listOrganizationsForIdentity, userID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []Organization{}
	for rows.Next() {
		var i Organization
		if err := rows.Scan(
			&i.ID,
			&i.Name,
			&i.DisplayName,
			&i.Annotations,
			&i.State,
			&i.Etag,
			&i.Revision,
			&i.CreatedBy,
			&i.UpdatedBy,
			&i.DeletedBy,
			&i.CreateTime,
			&i.UpdateTime,
			&i.DeleteTime,
			&i.PurgeTime,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const listSoleOwnerOrgsForIdentity = `-- name: ListSoleOwnerOrgsForIdentity :many
SELECT o.id, o.name, o.display_name, o.annotations, o.state, o.etag, o.revision, o.created_by, o.updated_by, o.deleted_by, o.create_time, o.update_time, o.delete_time, o.purge_time
  FROM organizations o
 WHERE o.delete_time IS NULL
   AND o.id IN (
     SELECT om.org_id
       FROM org_members om
       JOIN roles r ON r.id = om.role_id
      WHERE om.user_id IS NOT NULL
        AND r.is_system = true
        AND r.name = 'owner'
      GROUP BY om.org_id
     HAVING count(*) = 1
        AND bool_or(om.user_id = $1) = true
   )
   AND NOT EXISTS (
     SELECT 1
       FROM org_members om2
       JOIN roles r2 ON r2.id = om2.role_id
       JOIN groups g2 ON g2.id = om2.group_id
      WHERE om2.org_id = o.id
        AND r2.is_system = true
        AND r2.name = 'owner'
        AND g2.state = 'ACTIVE'
   )
`

// ListSoleOwnerOrgsForIdentity returns active orgs where the given
// identity is the ONLY owner. Used by DeleteAccount's VALIDATING
// phase to refuse deletion when the caller would leave any org
// without an owner.
//
// An org is "ONLY owned by this identity" iff:
//   - exactly one user-owner binding exists, and it points at this
//     identity, AND
//   - zero ACTIVE-group-owner bindings exist.
func (q *Queries) ListSoleOwnerOrgsForIdentity(ctx context.Context, userID pgtype.UUID) ([]Organization, error) {
	rows, err := q.db.Query(ctx, listSoleOwnerOrgsForIdentity, userID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []Organization{}
	for rows.Next() {
		var i Organization
		if err := rows.Scan(
			&i.ID,
			&i.Name,
			&i.DisplayName,
			&i.Annotations,
			&i.State,
			&i.Etag,
			&i.Revision,
			&i.CreatedBy,
			&i.UpdatedBy,
			&i.DeletedBy,
			&i.CreateTime,
			&i.UpdateTime,
			&i.DeleteTime,
			&i.PurgeTime,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}
