// Copyright 2025 Pivox
//
// Licensed under the Apache License, Version 2.0 (the "License");
// you may not use this file except in compliance with the License.
// You may obtain a copy of the License at
//
//     http://www.apache.org/licenses/LICENSE-2.0
//
// Unless required by applicable law or agreed to in writing, software
// distributed under the License is distributed on an "AS IS" BASIS,
// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
// See the License for the specific language governing permissions and
// limitations under the License.

// Code generated by protoc-gen-go-grpc. DO NOT EDIT.
// versions:
// - protoc-gen-go-grpc v1.6.1
// - protoc             (unknown)
// source: pivox/api/v1/sso.proto

package apiv1

import (
	context "context"
	grpc "google.golang.org/grpc"
	codes "google.golang.org/grpc/codes"
	status "google.golang.org/grpc/status"
)

// This is a compile-time assertion to ensure that this generated file
// is compatible with the grpc package it is being compiled against.
// Requires gRPC-Go v1.64.0 or later.
const _ = grpc.SupportPackageIsVersion9

const (
	Sso_ResolveProvider_FullMethodName = "/pivox.api.v1.Sso/ResolveProvider"
)

// SsoClient is the client API for Sso service.
//
// For semantics around ctx use and closing/ending streaming RPCs, please refer to https://pkg.go.dev/google.golang.org/grpc/?tab=doc#ClientConn.NewStream.
//
// Sso is a small, pre-authentication service that hosts the lookup
// front-channel (`Resolve`) used by the sign-in screen before any
// user identity exists. Authenticated SSO config CRUD lives on the
// Organizations service (Get/UpdateSsoConfig) — that's an AIP-156
// singleton sub-resource of Organization.
//
// Security profile (uniform across this service):
//   - No Firebase auth required (callers don't have an identity yet).
//   - Per-IP rate-limited at the middleware layer; surfaces as
//     RESOURCE_EXHAUSTED.
//   - Information-disclosure aware: returns NOT_FOUND for unknown
//     domains rather than echoing details back.
type SsoClient interface {
	// Maps an email address to its SSO provider id, if any. Used by the
	// sign-in screen to decide whether to redirect the user to an
	// identity provider before attempting password sign-in.
	//
	// Returns NOT_FOUND if no SSO is configured for the email's domain
	// (or if the SsoConfig is `enabled=false`). Returns RESOURCE_EXHAUSTED
	// if the caller is over the per-IP rate limit.
	//
	// (-- api-linter: core::0136::http-uri-suffix=disabled
	//
	//	aip.dev/not-precedent: ResolveProvider is a service-level
	//	(collection-less) custom method; the URL pattern intentionally
	//	has no parent. --)
	ResolveProvider(ctx context.Context, in *ResolveProviderRequest, opts ...grpc.CallOption) (*ResolveProviderResponse, error)
}

type ssoClient struct {
	cc grpc.ClientConnInterface
}

func NewSsoClient(cc grpc.ClientConnInterface) SsoClient {
	return &ssoClient{cc}
}

func (c *ssoClient) ResolveProvider(ctx context.Context, in *ResolveProviderRequest, opts ...grpc.CallOption) (*ResolveProviderResponse, error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	out := new(ResolveProviderResponse)
	err := c.cc.Invoke(ctx, Sso_ResolveProvider_FullMethodName, in, out, cOpts...)
	if err != nil {
		return nil, err
	}
	return out, nil
}

// SsoServer is the server API for Sso service.
// All implementations must embed UnimplementedSsoServer
// for forward compatibility.
//
// Sso is a small, pre-authentication service that hosts the lookup
// front-channel (`Resolve`) used by the sign-in screen before any
// user identity exists. Authenticated SSO config CRUD lives on the
// Organizations service (Get/UpdateSsoConfig) — that's an AIP-156
// singleton sub-resource of Organization.
//
// Security profile (uniform across this service):
//   - No Firebase auth required (callers don't have an identity yet).
//   - Per-IP rate-limited at the middleware layer; surfaces as
//     RESOURCE_EXHAUSTED.
//   - Information-disclosure aware: returns NOT_FOUND for unknown
//     domains rather than echoing details back.
type SsoServer interface {
	// Maps an email address to its SSO provider id, if any. Used by the
	// sign-in screen to decide whether to redirect the user to an
	// identity provider before attempting password sign-in.
	//
	// Returns NOT_FOUND if no SSO is configured for the email's domain
	// (or if the SsoConfig is `enabled=false`). Returns RESOURCE_EXHAUSTED
	// if the caller is over the per-IP rate limit.
	//
	// (-- api-linter: core::0136::http-uri-suffix=disabled
	//
	//	aip.dev/not-precedent: ResolveProvider is a service-level
	//	(collection-less) custom method; the URL pattern intentionally
	//	has no parent. --)
	ResolveProvider(context.Context, *ResolveProviderRequest) (*ResolveProviderResponse, error)
	mustEmbedUnimplementedSsoServer()
}

// UnimplementedSsoServer must be embedded to have
// forward compatible implementations.
//
// NOTE: this should be embedded by value instead of pointer to avoid a nil
// pointer dereference when methods are called.
type UnimplementedSsoServer struct{}

func (UnimplementedSsoServer) ResolveProvider(context.Context, *ResolveProviderRequest) (*ResolveProviderResponse, error) {
	return nil, status.Error(codes.Unimplemented, "method ResolveProvider not implemented")
}
func (UnimplementedSsoServer) mustEmbedUnimplementedSsoServer() {}
func (UnimplementedSsoServer) testEmbeddedByValue()             {}

// UnsafeSsoServer may be embedded to opt out of forward compatibility for this service.
// Use of this interface is not recommended, as added methods to SsoServer will
// result in compilation errors.
type UnsafeSsoServer interface {
	mustEmbedUnimplementedSsoServer()
}

func RegisterSsoServer(s grpc.ServiceRegistrar, srv SsoServer) {
	// If the following call panics, it indicates UnimplementedSsoServer was
	// embedded by pointer and is nil.  This will cause panics if an
	// unimplemented method is ever invoked, so we test this at initialization
	// time to prevent it from happening at runtime later due to I/O.
	if t, ok := srv.(interface{ testEmbeddedByValue() }); ok {
		t.testEmbeddedByValue()
	}
	s.RegisterService(&Sso_ServiceDesc, srv)
}

func _Sso_ResolveProvider_Handler(srv interface{}, ctx context.Context, dec func(interface{}) error, interceptor grpc.UnaryServerInterceptor) (interface{}, error) {
	in := new(ResolveProviderRequest)
	if err := dec(in); err != nil {
		return nil, err
	}
	if interceptor == nil {
		return srv.(SsoServer).ResolveProvider(ctx, in)
	}
	info := &grpc.UnaryServerInfo{
		Server:     srv,
		FullMethod: Sso_ResolveProvider_FullMethodName,
	}
	handler := func(ctx context.Context, req interface{}) (interface{}, error) {
		return srv.(SsoServer).ResolveProvider(ctx, req.(*ResolveProviderRequest))
	}
	return interceptor(ctx, in, info, handler)
}

// Sso_ServiceDesc is the grpc.ServiceDesc for Sso service.
// It's only intended for direct use with grpc.RegisterService,
// and not to be introspected or modified (even as a copy)
var Sso_ServiceDesc = grpc.ServiceDesc{
	ServiceName: "pivox.api.v1.Sso",
	HandlerType: (*SsoServer)(nil),
	Methods: []grpc.MethodDesc{
		{
			MethodName: "ResolveProvider",
			Handler:    _Sso_ResolveProvider_Handler,
		},
	},
	Streams:  []grpc.StreamDesc{},
	Metadata: "pivox/api/v1/sso.proto",
}
