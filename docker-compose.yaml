services:
  postgres:
    image: postgres:17
    environment:
      POSTGRES_USER: pivox
      POSTGRES_PASSWORD: pivox
      POSTGRES_DB: pivox
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
