PIVOX_API_URL=http://localhost:8080
# This is the shared secret used in development mode for the Pivox API
# SHARED_SECRET is removed in production builds for both Firebase Functions and the Pivox API
# Added to source control for development purposes only
SHARED_SECRET=dev-secret
